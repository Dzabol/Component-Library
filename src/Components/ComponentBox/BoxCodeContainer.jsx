import React from "react";

import "../../CSS/Components/BoxCodeContainer.css";

export default function BoxCodeContainer({ children }) {
  return <div className="codeWindow-container">{children}</div>;
}
